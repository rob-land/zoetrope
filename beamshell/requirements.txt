# Core GL/IO/app dependencies (the pure-logic core needs none of these).
moderngl>=5.8
glfw>=2.5
Pillow>=9.0

# Optional — install as needed:
#   python-mpv>=1.0     # 3D movie player (also needs system libmpv: `dnf install mpv-libs`)
#   pyudev>=0.24        # `beamshell watch` hotplug daemon
#   pillow-heif>=0.13   # read HEIC spatial photos from the Beam Pro
